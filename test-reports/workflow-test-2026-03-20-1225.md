# Dialed Workflow Test Report

---

### Run metadata

```
Date/time:       2026-03-20 12:25 PST (19:25 UTC)
Stack version:   43e2911 fix(frontend): add DesktopSidebar to AppLayout and restore Tailwind breakpoints
Tester:          Claude Code workflow-test agent
Environment:     local Docker Compose
```

---

### Results by step

```
STEP 1 — User registration and authentication
Status: PARTIAL
Duration: ~10s
Notes:
  - Original test email (ryan.shu.143@test.mototuner.local) rejected by email validation.
    Had to use ryan.shu.143@testmototuner.com instead.
  - Registration returns 201 with user_id and token — PASS
  - Login returns 200 with token and refresh_token — PASS
  - Profile returns skill_level: "novice" despite registering with "expert" — FAIL
  - Duplicate registration returns 422/VALIDATION_ERROR instead of 409/USER_EXISTS — FAIL
Evidence:
  Register response: {"user_id":"59600a68-...","token":"eyJ...","refresh_token":"eyJ..."}  HTTP 201
  Profile response:  {"skill_level":"novice","rider_type":"street","units":"metric"} — expected skill_level: "expert"
  Duplicate response: {"error":"Email already registered","code":"VALIDATION_ERROR"} HTTP 422 — expected 409/USER_EXISTS
```

```
STEP 2 — Bike creation
Status: PARTIAL
Duration: ~5s
Notes:
  - Test data suspension_spec rejected without schema_version field (not documented in test spec).
  - After adding schema_version: 1, bike created with 201 — PASS
  - CRITICAL: suspension_spec detailed fields (brand, spring_rate_nmm, compression_clicks,
    rebound_clicks, preload_turns, fork_height_mm) are silently dropped. API accepts them
    but stores a different normalized schema with generic field names (compression, rebound,
    preload, spring_rate, oil_level, ride_height) — all returned as null.
  - Unauthenticated access returns 401 — PASS
  - Bike appears in GET /garage/bikes list — PASS
Evidence:
  Sent: "front": {"brand":"Ohlins FKR","spring_rate_nmm":10.75,"compression_clicks":16,...}
  Got:  "front": {"compression":null,"rebound":null,"preload":null,"spring_rate":null,...}
```

```
STEP 3 — Track and event creation
Status: PARTIAL
Duration: ~5s
Notes:
  - Track created with 201 — PASS
  - Event creation: test spec uses flat fields (conditions: "dry", air_temp_c: 18, track_temp_c: 28)
    but API expects conditions as a nested ConditionsModel object. Had to restructure.
  - After adaptation, event created with 201 — PASS
  - Event correctly links bike_id and track_id — PASS
  - GET /garage/events?bike_id=$BIKE_ID returns the event — PASS
  - Response schema difference: no venue or ride_location in list response
Evidence:
  Event conditions accepted: {"condition":"dry","temp_c":18.0,"track_temp_c":28.0}
```

```
STEP 4 — Session creation with setup snapshot (all 4 sessions)
Status: PARTIAL
Duration: ~15s
Notes:
  - All 4 sessions created successfully — PASS
  - Session schema differences from test spec:
    * best_lap_ms → manual_best_lap_ms
    * tire_front/tire_rear are TireSnapshot objects, not strings
    * symptoms field does not exist in schema
    * setup_snapshot is not part of session creation (separate endpoint)
  - Snapshot endpoint returns 201 on both first and second call — PASS (append-only confirmed)
  - Fetching session detail returns snapshots array with 2 entries for S1 — PASS
  - Change log endpoint returns 201 and changes persist — PASS
  - Redis stream session.created not found (only dialed:ai key exists) — FAIL
Evidence:
  Session 1: 7615c0c4-a1a3-4f49-852a-7da496727d4d (practice, 110023ms)
  Session 2: 782ac140-cb02-4fe1-9f30-f71d4e256417 (practice, 126998ms)
  Session 3: 69da4bef-a4a7-4100-86e8-75bce244ae77 (practice, 107337ms)
  Session 4: 93ca9a46-003c-4d90-b08f-9299314ed6e1 (qualifying, 105972ms)
  Snapshots for S1: 2 (append-only confirmed)
  Redis keys: only "dialed:ai" — no session.created stream
```

```
STEP 5 — Telemetry CSV upload
Status: BLOCKED
Duration: N/A
Notes:
  - MISSING_FIXTURE: aim_csv_files — no CSV test data exists at tests/fixtures/aim_csvs/
  - Verified endpoints exist and respond correctly:
    * POST /ingest/csv returns 422 when missing required fields (endpoint alive)
    * GET /telemetry/{session_id}/channels returns 404 "No telemetry data" (correct)
    * GET /telemetry/{session_id}/analysis returns 404 "No telemetry data" (correct)
Evidence:
  Ingest endpoint validation: {"error":"body -> session_id: Field required","code":"VALIDATION_ERROR"}
```

```
STEP 6 — OCR setup sheet scan
Status: BLOCKED
Duration: N/A
Notes:
  - MISSING_FIXTURE: setup_sheet_image — no test image exists at tests/fixtures/setup_sheet_sample.jpg
  - Verified endpoint exists: POST /ingest/ocr returns 422 when file missing (endpoint alive)
Evidence:
  OCR endpoint validation: {"error":"body -> file: Field required","code":"VALIDATION_ERROR"}
```

```
STEP 7 — Voice feedback input
Status: FAIL
Duration: ~5s
Notes:
  - Test spec assumes a JSON transcript mode (POST with {"session_id","transcript"} body).
  - Actual API only accepts audio file upload via multipart form (file=@audio.wav).
  - No text/transcript-only endpoint exists.
  - The voice endpoint requires an audio file with extensions: .wav, .mp3, .m4a, .ogg, .webm
Evidence:
  POST /ingest/voice with JSON body: {"error":"body -> session_id: Field required"} HTTP 422
  (Form-based endpoint, not JSON — session_id must be Form field, and file is required)
```

```
STEP 8 — AI suggestion generation
Status: FAIL
Duration: 60s (timeout)
Notes:
  - POST /suggest returns 202 with job_id — PASS (job enqueued)
  - CRITICAL: AI worker is NOT running. The docker-compose only starts uvicorn (web server)
    for the AI service. The worker.py (queue consumer) is never launched.
  - Job sits in Redis dialed:ai queue indefinitely (2 jobs stacked up).
  - GET /suggest/session/{id} returns empty array [] after 60s polling.
  - GET /suggest/job/{job_id} returns 404 (no job tracking endpoint).
Evidence:
  Redis queue: LLEN dialed:ai = 2 (jobs queued but never consumed)
  AI container runs only: "uvicorn main:app --host 0.0.0.0 --port 8003"
  worker.py exists at services/ai/worker.py but is not started
```

```
STEP 9 — Applying and tracking suggestion changes
Status: BLOCKED
Duration: N/A
Notes:
  - Blocked by Step 8 failure — no suggestion exists to apply/track changes on.
  - Cannot test PATCH /suggest/{id}/changes/{change_id} without a suggestion.
```

```
STEP 10 — Progress dashboard
Status: PASS
Duration: ~3s
Notes:
  - GET /progress returns 200 with correct data — PASS
  - Lap time trend: [110023, 126998, 107337, 105972] in chronological order — PASS
  - Best lap: 105972 (Session 4 at Buttonwillow) — PASS
  - total_time_found_ms: 4051 — PASS
  - 1 track entry for Buttonwillow — PASS
  - Efficacy endpoint returns 200 with zero suggestions — PASS (expected given Step 8 failure)
  - Minor: response field names differ from test spec (lap_time_trend vs sessions,
    best_laps_by_track vs tracks) — not a bug, just spec drift
Evidence:
  {"lap_time_trend":[...4 entries...],"best_laps_by_track":[{..."best_lap_ms":105972...}],"total_time_found_ms":4051}
```

```
STEP 11 — Skill level adaptation
Status: BLOCKED
Duration: N/A
Notes:
  - PATCH /auth/me accepts skill_level change (novice/expert) — PASS
  - Cannot test novice vs expert suggestion output because AI worker is not running (Step 8).
  - Skill level update and restore works correctly.
Evidence:
  PATCH to novice: {"skill_level":"novice"} HTTP 200
  PATCH to expert: {"skill_level":"expert"} HTTP 200
```

```
STEP 12 — Error handling and edge cases
Status: PARTIAL
Duration: ~5s
Notes:
  - 12a: Invalid session ID → 202 accepted (FAIL — should be 404/SESSION_NOT_FOUND).
    The suggest endpoint enqueues without validating session existence.
  - 12b: Other user bike access → 403 FORBIDDEN (FAIL — should be 404 to not reveal existence).
  - 12c: Malformed suspension_spec → 422 VALIDATION_ERROR (PASS).
  - 12d: Non-CSV upload → 422 VALIDATION_ERROR (PARTIAL — expected 400 with INVALID_FILE_FORMAT code).
  - 12e: Fake token → 401 UNAUTHORIZED (PASS).
  - No 500 errors from any test — PASS.
  - All errors use standard envelope {error, code, request_id} — PASS.
Evidence:
  12a: {"job_id":"ef15e868-..."} HTTP 202 — expected 404
  12b: {"error":"You do not have access to this bike","code":"FORBIDDEN"} HTTP 403 — expected 404
  12c: {"error":"body -> suspension_spec -> schema_version: Field required","code":"VALIDATION_ERROR"} HTTP 422
  12d: {"error":"Unsupported file type: .md. Allowed: .csv, .txt","code":"VALIDATION_ERROR"} HTTP 422
  12e: {"error":"Invalid internal token: ...","code":"UNAUTHORIZED"} HTTP 401
```

---

### Issues found

```
ISSUE-1
Severity:    HIGH
Service:     auth
Step found:  1
Title:       Registration ignores skill_level field — defaults to novice
Expected:    Registration with skill_level: "expert" should store "expert" in profile
Actual:      Profile shows skill_level: "novice" after registering with "expert"
Request:     POST /api/v1/auth/register with {"skill_level":"expert",...}
Response:    GET /auth/me returns {"skill_level":"novice"}
Fix hint:    The register endpoint likely doesn't pass skill_level to the user creation query.
             Check services/core-api/routers/auth.py or services/core-api/services/auth_service.py.
```

```
ISSUE-2
Severity:    MEDIUM
Service:     auth
Step found:  1
Title:       Duplicate registration returns 422/VALIDATION_ERROR instead of 409/USER_EXISTS
Expected:    HTTP 409 with code: "USER_EXISTS"
Actual:      HTTP 422 with code: "VALIDATION_ERROR" and message "Email already registered"
Request:     POST /api/v1/auth/register (duplicate email)
Response:    {"error":"Email already registered","code":"VALIDATION_ERROR"} HTTP 422
Fix hint:    Change the duplicate email handler to return 409 with USER_EXISTS code.
```

```
ISSUE-3
Severity:    MEDIUM
Service:     auth
Step found:  1
Title:       Email validation rejects .local TLD — breaks test identity
Expected:    Accept test emails with .local domain (or provide test mode)
Actual:      422 "value is not a valid email address: The part after the @-sign is a special-use or reserved name"
Request:     POST /api/v1/auth/register with email "ryan.shu.143@test.mototuner.local"
Response:    422 VALIDATION_ERROR
Fix hint:    Either relax email validation in dev/test mode or update test spec to use standard domains.
```

```
ISSUE-4
Severity:    CRITICAL
Service:     garage
Step found:  2
Title:       Suspension spec detailed fields silently dropped — data loss
Expected:    Fields like brand, spring_rate_nmm, compression_clicks, rebound_clicks,
             preload_turns, fork_height_mm should be stored and returned intact
Actual:      All submitted detailed fields are silently discarded. The stored schema uses
             generic field names (compression, rebound, preload, spring_rate, oil_level,
             ride_height) which are all null because the input field names don't match.
Request:     POST /api/v1/garage/bikes with suspension_spec containing named fields
Response:    GET returns {"front":{"compression":null,"rebound":null,...}} — all null
Fix hint:    The SuspensionSpec Pydantic model uses generic field names that don't match
             the domain-specific names in the test data. Either update the model to accept
             both forms, or document the exact field mapping. The test spec fields
             (compression_clicks, rebound_clicks, etc.) are more descriptive and should
             be the canonical names.
```

```
ISSUE-5
Severity:    CRITICAL
Service:     ai
Step found:  8
Title:       AI worker not running — suggestion jobs never processed
Expected:    AI worker consumes from dialed:ai Redis queue and generates suggestions
Actual:      Docker compose only starts uvicorn web server. worker.py is never launched.
             Jobs accumulate in Redis queue indefinitely (LLEN dialed:ai = 2).
Request:     POST /api/v1/suggest {"session_id":"..."} → 202 (enqueued but never processed)
Response:    GET /suggest/session/{id} returns [] forever
Fix hint:    Add a separate service/container in docker-compose.yml for the AI worker,
             or run worker.py as a background task in the AI service startup.
             worker.py entry point: worker.run() consuming from "dialed:ai" queue.
```

```
ISSUE-6
Severity:    HIGH
Service:     ai
Step found:  12a
Title:       POST /suggest accepts invalid session IDs without validation
Expected:    404 with code: "SESSION_NOT_FOUND" for non-existent session
Actual:      202 accepted — job enqueued for a session that doesn't exist
Request:     POST /api/v1/suggest {"session_id":"00000000-0000-0000-0000-000000000000"}
Response:    {"job_id":"ef15e868-..."} HTTP 202
Fix hint:    Validate session existence (and user ownership) before enqueuing the job.
```

```
ISSUE-7
Severity:    MEDIUM
Service:     garage
Step found:  12b
Title:       Other user bike access returns 403 FORBIDDEN instead of 404
Expected:    404 — do not reveal that the resource exists to unauthorized users
Actual:      403 with "You do not have access to this bike" — confirms the bike exists
Request:     GET /api/v1/garage/bikes/{id} with another user's token
Response:    {"error":"You do not have access to this bike","code":"FORBIDDEN"} HTTP 403
Fix hint:    Change the authorization check to return 404 NOT_FOUND for resources
             not owned by the requesting user.
```

```
ISSUE-8
Severity:    LOW
Service:     ingestion
Step found:  12d
Title:       Non-CSV upload returns VALIDATION_ERROR instead of INVALID_FILE_FORMAT
Expected:    HTTP 400 with code: "INVALID_FILE_FORMAT"
Actual:      HTTP 422 with code: "VALIDATION_ERROR"
Request:     POST /api/v1/ingest/csv with file=@README.md
Response:    {"error":"Unsupported file type: .md","code":"VALIDATION_ERROR"} HTTP 422
Fix hint:    Return 400 with code "INVALID_FILE_FORMAT" for wrong file type uploads.
```

```
ISSUE-9
Severity:    HIGH
Service:     ingestion
Step found:  7
Title:       Voice endpoint has no text/transcript mode — only accepts audio files
Expected:    Ability to POST a JSON body with {"session_id","transcript"} for text-only input
Actual:      Only accepts multipart form with audio file upload (.wav, .mp3, .m4a, .ogg, .webm)
Request:     POST /api/v1/ingest/voice with JSON body
Response:    422 — session_id must be a form field, file is required
Fix hint:    Add a separate endpoint (e.g., POST /ingest/voice/transcript) that accepts
             pre-transcribed text for entity extraction without requiring audio.
```

```
ISSUE-10
Severity:    HIGH
Service:     session
Step found:  4
Title:       No session.created Redis event emitted
Expected:    session.created event in Redis Streams after session creation
Actual:      Only "dialed:ai" key exists in Redis — no session.created stream
Request:     N/A (event emission is server-side)
Response:    redis-cli KEYS * → only "dialed:ai"
Fix hint:    Add Redis stream publish in the session creation service layer.
```

```
ISSUE-11
Severity:    HIGH
Service:     gateway / test-spec
Step found:  3, 4
Title:       Test data schema mismatch — multiple fields differ from actual API schemas
Expected:    Test spec data matches API schemas exactly
Actual:      Multiple mismatches require adaptation:
             - Event conditions: string → ConditionsModel object
             - Session best_lap_ms → manual_best_lap_ms
             - Session tire_front/tire_rear: string → TireSnapshot object
             - Session symptoms field: does not exist
             - Suspension spec fields: domain names vs generic names
Request:     N/A — test specification issue
Response:    N/A
Fix hint:    Update the workflow-test.md test data to match current API schemas,
             OR update the API schemas to match the domain-specific test data.
```

```
ISSUE-12
Severity:    MEDIUM
Service:     test-infra
Step found:  5, 6
Title:       Missing test fixture files — CSV and setup sheet image
Expected:    tests/fixtures/aim_csvs/{11,12,13,14}.csv and tests/fixtures/setup_sheet_sample.jpg
Actual:      tests/fixtures/ directory does not exist
Request:     N/A
Response:    N/A
Fix hint:    Create the fixture directory and add sample AiM CSV files and a setup sheet image.
```

---

### Summary

```
Total steps:      12
Passed:           1    (Step 10)
Failed:           2    (Steps 7, 8)
Partial:          5    (Steps 1, 2, 3, 4, 12)
Blocked:          4    (Steps 5, 6, 9, 11)

Critical issues:  2    (ISSUE-4: suspension spec data loss, ISSUE-5: AI worker not running)
High issues:      5    (ISSUE-1, ISSUE-6, ISSUE-9, ISSUE-10, ISSUE-11)
Medium issues:    3    (ISSUE-2, ISSUE-7, ISSUE-12)
Low issues:       2    (ISSUE-3, ISSUE-8)

Recommendation:   NEEDS FIXES FIRST
```

---

## Handoff to main context

```
WORKFLOW TEST COMPLETE — HANDOFF REPORT

Run: 2026-03-20-1225
Overall status: FAIL

Issues by service (paste full ISSUE blocks for each):

  → auth/       3 issues (ISSUE-1, ISSUE-2, ISSUE-3)    Priority: HIGH
  → garage/     2 issues (ISSUE-4, ISSUE-7)              Priority: CRITICAL
  → session/    1 issue  (ISSUE-10)                       Priority: HIGH
  → ingestion/  2 issues (ISSUE-8, ISSUE-9)              Priority: HIGH
  → telemetry/  0 issues                                  Priority: N/A
  → ai/         2 issues (ISSUE-5, ISSUE-6)              Priority: CRITICAL
  → progress/   0 issues                                  Priority: N/A
  → gateway/    1 issue  (ISSUE-11)                       Priority: HIGH
  → test-infra  1 issue  (ISSUE-12)                       Priority: MEDIUM

Sub-agent dispatch:
  Agent 2 (auth/garage):    ISSUE-1, ISSUE-2, ISSUE-3, ISSUE-4, ISSUE-7
  Agent 3 (session):        ISSUE-10
  Agent 4 (ingestion):      ISSUE-8, ISSUE-9
  Agent 5 (telemetry):      (none)
  Agent 6 (ai/progress):    ISSUE-5, ISSUE-6
  Agent 1 (gateway):        ISSUE-11
  Test infra:               ISSUE-12

Blocking issues (must fix before next test run):
  ISSUE-5  — AI worker not running (no suggestions can be generated)
  ISSUE-4  — Suspension spec data loss (core domain data silently dropped)
  ISSUE-11 — Test data/API schema mismatch (must align before retest)

Non-blocking (fix in parallel):
  ISSUE-1  — Registration ignores skill_level
  ISSUE-2  — Duplicate registration wrong status code
  ISSUE-3  — Email validation rejects .local TLD
  ISSUE-6  — Suggest endpoint accepts invalid session IDs
  ISSUE-7  — Bike access returns 403 instead of 404
  ISSUE-8  — File upload wrong error code
  ISSUE-9  — No transcript-only voice endpoint
  ISSUE-10 — No session.created Redis event
  ISSUE-12 — Missing test fixture files
```
